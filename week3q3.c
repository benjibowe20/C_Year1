#include <stdio.h>

int main()
{
    float pi = 3.14;
    float radius = 4.8;

    float area = pi*(radius*radius);
    printf("the area of the circle is %f cm squared",area);

    return 0;
}
